//Write JavaScript code that retrieves all elements with the class "highlight" 
//and adds the CSS class "bold" to each of them.
//Explain the difference between getElementById and querySelector methods in the DOM.
// Provide examples demonstrating their usage and when to use each method.

const highlight = document.querySelectorAll("highlight")

console.log(highlight)

const element = document.getElementsByClassName("css").element.style.fontWeight = "lighter"


console.log(element)